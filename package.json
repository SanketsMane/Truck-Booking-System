{
  "name": "truckgee",
  "private": true,
  "version": "0.1.0",
  "description": "Truckgee — end-to-end tests (repo root; backend/ and frontend/ are separate workspaces with their own package.json)",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
