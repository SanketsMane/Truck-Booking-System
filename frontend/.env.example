# Copy to .env (or .env.local) and point at your running backend.
VITE_API_URL=http://localhost:3000

# Free public token from https://account.mapbox.com/access-tokens/ — powers
# the live truck-tracking map. Restrict it to your domain(s) in the Mapbox
# dashboard once deployed.
VITE_MAPBOX_TOKEN=
